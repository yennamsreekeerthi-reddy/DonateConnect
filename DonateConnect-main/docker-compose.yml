version: '3.8'
services:
  mongo:
    image: mongo:6
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: donateconnect
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped
volumes:
  mongo_data:
    driver: local
